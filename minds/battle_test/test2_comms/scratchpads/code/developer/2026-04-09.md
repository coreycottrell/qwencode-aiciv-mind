
## [14:33:18]

Task: Write a Python function that computes fibonacci(n) recursively with memoization. Return just the code.
Result: ```python
def fibonacci(n, memo={}):
    if n in memo:
        return memo[n]
    if n <= 2:
        return 1
    memo[n] = fibonacci(n - 1, memo) + fibonacci(n - 2, memo)
    return memo[n]
```

## [14:33:52]

Task: Add type hints and a docstring to your fibonacci function.
Result: Here's the updated function with type hints and a docstring:

```python
from typing import Dict

def fibonacci(n: int, memo: Dict[int, int] = None) -> int:
    """
    Compute the nth Fibonacci number
